extends layouts/main

block content
    .form-container
        form(action=`/users${routes.changePassword}`, method="post")
        input(type="password", name="oldPasswod", plceholder="Current Password")
        input(type="password", name="newPassword", plceholder="New Password")
        input(type="password", name="newPassword1", plceholder="Verify New Password")
        input(type="submit", value="Change Password")